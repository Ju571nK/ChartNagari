# ─── 환경 변수 (절대 커밋 금지) ───
.env
.env.local
.env.*.local
!.env.example

# ─── Go ───
# Binaries (root or bin/)
/chart-analyzer
/server
/chatter
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test & coverage
*.test
*.out
coverage.html
coverage.txt
__debug_bin

# Go workspace (go.work)
go.work
go.work.sum

# ─── Frontend (Vite + React + TypeScript) ───
web/node_modules/
web/dist/
web/.vite/
web/*.local
*.tsbuildinfo

# ─── SQLite & 로컬 데이터 ───
/data/*.db
/data/*.db-shm
/data/*.db-wal
/data/*.db-journal
*.db
*.db-shm
*.db-wal
/data/tiingo_state.json

# ─── 로그 & 임시 ───
*.log
logs/
tmp/
temp/
*.tmp
*.temp
*.swp
*.swo
*~

# ─── OS ───
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db

# ─── IDE / 에디터 ───
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Cursor (선택: 팀 공유 룰만 커밋할 경우 제거 가능)
# .cursor/

# Claude Code (폴더 전체 미추적)
.claude/
# ↑ .claude/agents/, .claude/settings.local.json 등 모두 포함
# 전역 팀 config는 ~/.claude/teams/ChartAnalysis-dev/ 에 저장됨

# ─── 앱 설정 파일 (API 키 등 민감 정보 포함) ───
config/settings.yaml

# ─── 기타 ───
# Local overrides for config (if any)
config/*.local.yaml
*.local.yaml
docs/research/
docs/community-posts.md
docs/best-of-algotrading-pr.md
.gstack/
